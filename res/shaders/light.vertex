#version 330 core
layout (location = 0) in vec3 position;
layout (location = 1) in vec3 normal;

uniform mat4 MVP;
uniform vec3 trans;

out vec3 Normal;
out vec3 point_pos;

void main() {
    gl_Position = MVP * vec4(position + trans, 1.0f);
    Normal = normal;
    point_pos = position + trans;
}