#version 330 core

#ifdef GL_ES
precision mediump float;
#endif

in vec3 ourColor;
in vec3 FancyColor;
out vec4 FragColor;

void main() {
    FragColor = vec4(ourColor + FancyColor, 1.0);
    //FragColor = vec4(ourColor + vec3(0.2, 0.3, 0.1), 1.0);
}